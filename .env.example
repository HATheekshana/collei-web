MONGODB_URI=
MONGODB_DATABASE=collei_site
# Generate two different values: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_PASSWORD=
SESSION_SECRET=
# Exact website origin, without trailing slash
SITE_ORIGIN=https://your-project.vercel.app
